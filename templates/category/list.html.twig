{% extends 'base.html.twig' %}

{% block title %}Liste des produits de la catégorie {{ category.name }}{% endblock %}

{% block body %}
{#    {{ dump() }}#}

    <main class="container">
        <section class="row">
            <div class="col-12">
                <h1>Liste des produits de la catégorie {{ category.name }}</h1>
            </div>

            {% for product in products.data %}
                <div class="col-6 mb-3">
                    <article class="card h-100">
                        <div class="card-body">
                            <h3 class="card-title">{{ product.name }}</h3>
                            <p class="card-text">{{ product.description }}</p>
                            <a href="{{ path('app_product_details', {"slug": product.slug}) }}" class="btn btn-outline-primary">Voir les détails</a>
                        </div>
                    </article>
                </div>
            {% endfor %}

            {% set path = 'app_category_list' %}
            {% set slug = category.slug %}
            {% set pages = products.pages %}
            {% set currentPage = products.page %}
            {% include '_partials/_pagination.html.twig' %}

        </section>
    </main>
{% endblock %}
